spring:
    datasource:
        url: ${DB_URL}
        username: ${PG_USER}
        password: ${PG_PASSWORD}
    jpa:
        hibernate:
            ddl-auto: none
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect

